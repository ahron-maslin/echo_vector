{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "git add -A && git diff --cached --quiet || git commit -m 'auto: post-edit commit' 2>/dev/null || true",
            "statusMessage": "Committing changes..."
          }
        ]
      }
    ]
  }
}
